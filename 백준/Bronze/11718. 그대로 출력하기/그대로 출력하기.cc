#include <bits/stdc++.h>
using namespace std;
int main()
{
//    cin.tie();
//    ios_base::sync_with_stdio(0);
    char*arr=(char*)malloc(101);
    while(gets(arr)!=NULL){
        puts(arr);
    }
}
